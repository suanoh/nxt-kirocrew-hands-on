# 설명

- **에이전트 · 스킬 이름**: Kiro(KiroCrew 에이전트) / `signup-check` 스킬 + `tally_signups.py` 집계 코드.
- **정리 기준**: 참가만 집계하고 불참은 제외, 간식은 1인 1개(받지 않음=0), 참가자 빈칸은 미응답으로 따로 표시(임의로 안 채움), 생수는 참가 인원수, 정원 12 초과 여부 확인.
- **테스트 후 수정한 점**: PowerShell로 저장한 집계 JSON의 한글이 깨져서, Python이 직접 UTF-8로 파일에 쓰도록 저장 방식을 바꿈.
